feat: port channel pricing (fees) from upstream; add panel UI

The addon had no channel pricing at all: no /api/fees endpoint and no UI, even
though the underlying FetchModelPricing now exists (restored in the previous
upstream sync). The rates box the user saw came from the upstream Windows build
(its FeesInfo note text and cached_at 08-30 15:53 match upstream data/).

Ported from upstream app.go (FeesInfo / RefreshPricing / loadPricingCache /
savePricingCache / staticFeesInfo), adapted to the addon svc.Runtime layout:

- internal/svc: add pricing cache fields (pricingFP/pricingCache/pricingFetched/
  pricingErr with a mutex) and the four pricing methods; Runtime.loadPricing()
  is called from New() so a cached rates file is reused across restarts.
- cmd/serverd: serve GET /api/fees and POST /api/fees/refresh by wrapping the
  existing server.Handler (the Handler only takes server.Config, which does not
  carry the Runtime). Everything else still falls through to the Handler.
- login_ui.py: new "费率" tab with a per-channel table (two models per row, like
  upstream), a 刷新费率 button, note/disclaimer/last-updated lines, and error
  display; panel proxies to serverd via SRVD_UPSTREAM.

Pricing cache file: <state dir>/pricing-cache.json, refreshed lazily when older
than 1h and on demand via the refresh button.

Verified: go build ./... and go vet ./internal/... ./cmd/... pass; Python syntax
of login_ui.py verified (ast.parse).